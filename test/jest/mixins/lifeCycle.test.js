import { unwatchFile } from 'fs';

/*
 * import should from 'should';
import lifeCycle from '@devoinc/applications-builder/mixins/lifeCycle';

let data = require('./data/lifeCycle.json');

describe('Widget LifeCycle:', () => {

  describe('Check the normalize function:', () => {

    let obj = lifeCycle({});
    let normal = obj.normalize(data);

    it('Check that the base data return 2 elements', () => {
      should(normal.length).be.equal(2);
    });

    it('Check that the base data return first element as dataMatrix', () => {
      should.exist(normal[0].dataMatrix);
    });

    it('Check that the base data return last element not as dataMatrix', () => {
      should.not.exist(normal[1].dataMatrix);
    });

  });

});
*/

test('empty', () => {
  return undefined;
});
